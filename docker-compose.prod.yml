services:
  subdomainly-app:
    build: 
      context: .
      dockerfile: Dockerfile.prod
    env_file:
      - .env.prod
    networks:
      - npm_networks

networks:
  nginx-proxy-manager_default:
    external: true